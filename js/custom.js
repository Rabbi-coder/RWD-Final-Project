// type js start
var typed = new Typed(".auto_type", {
    strings: ["DESIGNER", "DEVELOPER", "FREELANCER"],
    typeSpeed: 80,
    backSpeed: 80,
    loop: true
})
// type js end

// mixit part start
var mixer = mixitup('.mixit');
// mixit part end

// lightbox part start
lightbox.option({
    'resizeDuration': 200,
    'wrapAround': true
})
// lightbox part end


// wow part start
new WOW().init();
// wow part end